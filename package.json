{
  "name": "freezeops",
  "version": "0.2.0",
  "private": true,
  "description": "Guardrails for AI-assisted development — deterministic safety rules for sensitive codebases",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @freezeops/core && npm run build -w @freezeops/cli",
    "typecheck": "npm run typecheck -w @freezeops/core && npm run typecheck -w @freezeops/cli",
    "validate": "npm run build -w @freezeops/core && npm run typecheck && npm run build -w @freezeops/cli"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
